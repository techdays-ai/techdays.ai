# Courses

Curricula composing guides into an ordered path. One file per course: `<slug>.md`.

Frontmatter: `title`, `description`, `audience`, `modules` (ordered list of guide slugs), optional `draft: true`.
