# Guides

Long-form docs — the atomic unit courses compose. One directory per guide: `<slug>/index.md` plus an optional `assets/` directory.

Frontmatter: `title`, `description`, `tags`, `level` (intro|intermediate|advanced), `duration`, optional `draft: true`.
